[package]
name         = "tronz-primitives"
description  = "Leaf primitive types for the tronz TRON SDK (Address, Trx, ResourceCode, signatures)."
keywords     = ["tron", "blockchain", "cryptocurrency", "primitives", "address"]
categories   = ["cryptography::cryptocurrencies", "network-programming", "api-bindings", "asynchronous"]
version      = { workspace = true }
edition      = { workspace = true }
rust-version = { workspace = true }
license      = { workspace = true }
repository   = { workspace = true }

[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs", "-Zunstable-options", "--generate-link-to-definition", "--show-type-layout"]

[dependencies]
alloy-primitives = { workspace = true }
k256             = { workspace = true }
bs58             = { workspace = true }
hex              = { workspace = true }
serde            = { workspace = true }
thiserror        = { workspace = true }
